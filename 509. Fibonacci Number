class Solution {
    public int fib(int n) {
       return find(n); 
    }
    static int find(int n){
        if(n<=1){
            return n;
        }
        int ans=find(n-1)+find(n-2);
        return ans;
    }
}
