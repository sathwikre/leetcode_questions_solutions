class Solution {
    public int maxProfit(int[] prices) {
        int[][] dp=new int[prices.length][2];
        for(int i=0;i<dp.length;i++){
            Arrays.fill(dp[i],-1);
        }
        return find(prices,0,1,dp);
    }
    static int find(int[] arr,int i,int buy,int[][] dp){
        if(i==arr.length) return 0;
        if(dp[i][buy]!=-1) return dp[i][buy];
        int profit=0;
        if(buy==1){
            profit= Math.max(-arr[i]+find(arr,i+1,0,dp),0+find(arr,i+1,1,dp));
        }else{
            profit=  Math.max(arr[i]+find(arr,i+1,1,dp),0+find(arr,i+1,0,dp));
        }
    return dp[i][buy]=profit;
    }
}
