class Solution {
    public int minimumOperations(int[] nums) {
        return find(nums);
    }
    static int find(int[] arr){
        int c=0;
        for(int i=0;i<arr.length;i++){
            c+=Math.min(arr[i]%3,3-arr[i]%3);
        }
return c;
    }
}
