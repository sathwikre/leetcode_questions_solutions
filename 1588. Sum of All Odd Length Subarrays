class Solution {
public int sumOddLengthSubarrays(int[] arr) {
        int sum =0;
        int n=arr.length;
        for(int i=0;i<arr.length;i++){
            int total =(i+1)*(n-i);
            int odd=(total+1)/2;
            sum+=odd*arr[i];
        }
        return sum;
    }
}