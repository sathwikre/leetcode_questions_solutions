class Solution {
    public int sumOfGoodIntegers(int n, int k) {
        int s=Math.max(1,n-k);
        int sum=0;
        for(int i=s;i<=n+k;i++){
            if(Math.abs(n-i)<=k&&(n&i)==0){
                sum+=i;
            }
        }
        return sum;
    }
}
