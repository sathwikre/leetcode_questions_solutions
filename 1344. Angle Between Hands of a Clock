class Solution {
    public double angleClock(int hour, int minutes) {
        double ans=30*hour-5.5*minutes;
        if(ans<0){
            ans=ans*-1;
        }
        if(ans>180){
            return Math.abs(360-ans);
        }
        return Math.abs(ans);
    }
}
