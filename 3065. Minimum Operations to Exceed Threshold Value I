class Solution {
    public int minOperations(int[] nums, int k) {
        return find(nums,k);
    }
    static int find(int[] arr,int t){
        int c=0;
        for(int i=0;i<arr.length;i++){
            if(arr[i]<t){
                c++;
            }
        }
        return c;
    }
}
