class Solution {
    public int singleNonDuplicate(int[] nums) {
        return find(nums);

    }
    static int find(int[] arr){
      int sum=0;
      for(int i=0;i<arr.length;i++){
        sum=sum^arr[i];
      }
        return sum;
    }
}
